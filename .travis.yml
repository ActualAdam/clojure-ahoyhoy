sudo: required
services:
 - docker
script:
 - docker build -t build-img -f ./Dockerfile.build .
 - docker create --name build-container build-img
 - mkdir ./target
 - docker cp build-container:/usr/src/app/app-snapshot.jar ./target/app-snapshot.jar
 - docker build . -t actualadam/clojure-ahoyhoy


